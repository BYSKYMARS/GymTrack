<% content_for :title, "Nueva Sala" %>

<div class="container">
  <h1 class="mb-4">Nueva Sala</h1>

  <div class="card">
    <div class="card-body">
      <%= render "form", room: @room %>
    </div>
  </div>

  <div class="mt-3">
    <%= link_to "Volver a la lista de salas", rooms_path, class: "btn btn-secondary" %>
  </div>
</div>
