<% content_for :title, "Nuevo Plan" %>

<div class="container mt-4">
  <h1 class="mb-4">Crear nuevo plan</h1>

  <div class="card">
    <div class="card-body">
      <%= render "form", plan: @plan %>
    </div>
  </div>

  <div class="mt-3">
    <%= link_to "Volver a los planes", plans_path, class: "btn btn-secondary" %>
  </div>
</div>
