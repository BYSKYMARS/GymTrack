<% content_for :title, "Editar pago" %>

<h1 class="mb-4">Editar pago</h1>

<div class="card mb-4">
  <div class="card-body">
    <%= render "form", payment: @payment %>
  </div>
</div>

<div class="d-flex justify-content-between">
  <%= link_to "Ver este pago", @payment, class: "btn btn-outline-primary" %>
  <%= link_to "Volver a la lista de pagos", payments_path, class: "btn btn-secondary" %>
</div>
