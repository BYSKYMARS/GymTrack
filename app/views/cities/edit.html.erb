<% content_for :title, "Editar Ciudad" %>

<div class="container mt-4">
  <h1 class="mb-4">Editar Ciudad</h1>

  <%= render "form", city: @city %>

  <div class="mt-4">
    <%= link_to "Ver ciudad", @city, class: "btn btn-outline-primary me-2" %>
    <%= link_to "Volver a ciudades", cities_path, class: "btn btn-secondary" %>
  </div>
</div>
