<% content_for :title, "Nueva ciudad" %>

<div class="container mt-4">
  <h1 class="mb-4">Nueva ciudad</h1>

  <div class="mb-4">
    <%= render "form", city: @city %>
  </div>

  <%= link_to "Volver a ciudades", cities_path, class: "btn btn-secondary" %>
</div>
