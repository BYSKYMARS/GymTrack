<% content_for :title, "Nueva Reserva" %>

<div class="container mt-4">
  <h1 class="mb-4">Nueva Reserva</h1>

  <div class="card">
    <div class="card-body">
      <%= render "form", reservation: @reservation %>
    </div>
  </div>

  <div class="mt-3">
    <%= link_to "Volver a Reservas", reservations_path, class: "btn btn-outline-secondary" %>
  </div>
</div>
