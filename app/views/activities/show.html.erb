<% if notice %>
  <div class="alert alert-success"><%= notice %></div>
<% end %>

<div class="container mt-4">
  <%= render @activity %>

  <div class="mt-3">
    <%= link_to "Editar esta actividad", edit_activity_path(@activity), class: "btn btn-primary me-2" %>
    <%= link_to "Volver a actividades", activities_path, class: "btn btn-secondary me-2" %>

    <%= button_to "Eliminar esta actividad", @activity, method: :delete, data: { confirm: "¿Seguro que quieres eliminar esta actividad?" }, class: "btn btn-danger" %>
  </div>
</div>
